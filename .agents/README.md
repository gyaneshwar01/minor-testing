# Agent Configuration

This directory contains AI agent configuration files for the Neplish ASR project.

- `rules/` — Project-wide rules and constraints agents must follow
- `skills/` — Reusable skill definitions for common project tasks
